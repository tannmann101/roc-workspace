VITE_USE_FIREBASE_EMULATOR=false

# Get a free key at https://aistudio.google.com/apikey -- see README for
# the HTTP-referrer restriction step. Without this set, the AI-assist
# buttons will show an error when clicked but the rest of the app works fine.
VITE_GEMINI_API_KEY=
